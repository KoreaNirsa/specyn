-- Generated by Specyn local runtime for sample-service
create table if not exists sample_service_records (
    id bigint generated always as identity primary key,
    external_id varchar(128) not null,
    payload json not null,
    created_at timestamp not null default current_timestamp,
    updated_at timestamp not null default current_timestamp,
    constraint uk_sample_service_external_id unique (external_id)
);

create index if not exists idx_sample_service_created_at on sample_service_records (created_at desc);
